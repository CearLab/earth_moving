# Custom Benchmark Configuration
# Edit these values to customize your comprehensive benchmark

# Grid sizes to test - RESTORED based on memory analysis showing safety
grid_sizes = [25, 35, 45, 65]

# Object counts to test - RESTORED - memory usage is actually very reasonable  
object_counts = [50, 75, 100, 125]

# Random seeds - RESTORED - seeds don't affect memory usage at all
seeds = [31, 42, 123, 456, 789]

# Iterations per scenario - can be restored to original
iterations = 5

# Estimated reduction: From ~12 hours to ~1-2 hours
# Original: 5 grid × 5 objects × 5 seeds × 10 iter = 1,250 base scenarios
# Custom:   2 grid × 2 objects × 2 seeds × 5 iter = 40 base scenarios
# Reduction factor: ~31x faster